/**
 * Canonical in-app route paths shared across the router, the post-login redirect
 * logic, and the pages themselves — so the destination is defined in exactly one place.
 */

/** Logged-in tools hub: the default post-login destination + the gate's redirect target. */
export const HUB_PATH = "/app";
